# 切换到author分支下，并保证本地已经同步了远端develop的最新代码。
git checkout author
git pull
# 把本地的author分支强制(-f)推送到远端master。
git push origin author:master -f
# 切换到旧分支master。
git checkout master
# 下载远程仓库最新内容，不做合并。
git fetch --all
# 把HEAD指向master最新版本。
git reset --hard origin/master
git pull
